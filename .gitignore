# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Ferramentas
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Build / empacotamento
build/
dist/
*.spec
!SidecarHF.spec
gui_web/release/
# Binário do llama-server (llama.cpp) baixado por scripts/preparar_llama.py —
# grande (~130 MB), materializado no build, nunca versionado (ADR-0016 §E).
resources/llama/

# IDE / SO
.vscode/
.idea/
Thumbs.db
.DS_Store

# Saídas geradas pelo app em desenvolvimento (exemplos versionados ficam em exemplos/)
*.xlsx
*.docx
!exemplos/*.xlsx
!exemplos/*.docx

# Segredos — nunca versionar (REQ-SEC-001)
.env
*.key
# Certificados de assinatura de codigo — o PFX de teste (ADR-0021/T-2402) fica
# fora do repo por padrao, mas *.pfx aqui e a segunda barreira contra vazamento.
*.pfx

# Orquestração local do Claude Code — não versionar (config da IDE, não artefato do produto)
.claude/
